{% extends 'base.html.twig' %}

{% block body %}
    <h1>Livre list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Titrelivre</th>
                <th>Noticelivre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for livre in livres %}
            <tr>
                <td><a href="{{ path('livre_show', { 'id': livre.id }) }}">{{ livre.id }}</a></td>
                <td>{{ livre.titreLivre }}</td>
                <td>{{ livre.noticeLivre }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('livre_show', { 'id': livre.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('livre_edit', { 'id': livre.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('livre_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
