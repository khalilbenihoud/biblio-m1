{% extends 'base.html.twig' %}

{% block body %}
    <h1>Exemplaire</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exemplaire.id }}</td>
            </tr>
            <tr>
                <th>Numeroexemplaire</th>
                <td>{{ exemplaire.numeroExemplaire }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('exemplaire_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('exemplaire_edit', { 'id': exemplaire.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
